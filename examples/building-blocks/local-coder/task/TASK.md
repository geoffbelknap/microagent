Make every test in this workspace pass.

`calculator.py` has bugs. Fix the implementation so that `pytest` passes,
including the divide-by-zero case, which must raise `ValueError`. Do not edit
the tests.
