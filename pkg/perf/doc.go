// Package perf measures workspace full readiness across cold boot, snapshot
// fork and restore, paused resume, and guest interfaces, plus runtime
// footprint, backing "microagent perf".
package perf
