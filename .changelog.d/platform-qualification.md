Linux and macOS Homebrew builds now advance independently. Mac promotion
requires live qualification of the selected source revision. Contributors can
validate a candidate in an isolated checkout with `qualify-applevf.py`.
